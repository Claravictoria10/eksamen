<section class="forside1" aria-label="Forside hero">
  <img class="hero-image" src="/img/herodk.webp" alt="To glade kvinder i bymiljø" />

  <div class="overlay-content">
    <h1>
      <span class="title-line">Her kan du finde alt hvad du skal vide om</span>
      <span class="title-line">Dankort Øremærket.</span>
    </h1>

    <div class="brand-block">
      <img src="/Images1/dankort.webp" alt="Dankort logo" />
      <p>Små handlinger. Stor betydning.</p>
    </div>
  </div>
</section>

<style>
  .forside1 {
    position: relative;
    width: min(1120px, calc(100% - 40px));
    min-height: clamp(360px, 52vw, 620px);
    margin: 22px auto 30px;
    border-radius: 26px;
    overflow: hidden;
    display: flex;
    align-items: stretch;
    box-shadow: 0 16px 42px rgba(0, 0, 0, 0.16);
  }

  .hero-image {
    width: 100%;
    height: 100%;
    min-height: clamp(360px, 52vw, 620px);
    object-fit: cover;
    object-position: center center;
    display: block;
    transform: scale(1.02);
  }

  .overlay-content {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    padding: clamp(18px, 3vw, 34px);
    color: #ffffff;
    background: linear-gradient(90deg, rgba(0, 0, 0, 0.5) 0%, rgba(0, 0, 0, 0.24) 40%, rgba(0, 0, 0, 0.03) 78%);
  }

  .overlay-content h1 {
    margin: 0;
    max-width: 16.5ch;
    font-size: 65px;
    font-weight: 700;
    line-height: 1.06;
    letter-spacing: -0.005em;
    word-spacing: 0.08em;
  }

  .overlay-content h1 .title-line {
    display: block;
  }

  .brand-block {
    display: flex;
    flex-direction: column;
    gap: 14px;
  }

  .brand-block img {
    width: clamp(120px, 11vw, 170px);
    height: auto;
  }

  .brand-block p {
    margin: 0;
    max-width: 18ch;
    font-size: 20px;
    line-height: 1.08;
    font-weight: 400;
  }

  @media (min-width: 900px) {
    .forside1,
    .hero-image {
      width: 100%;
      min-height: 0;
      height: 100vh;
      height: 100svh;
      height: 100dvh;
      margin: 0;
      border-radius: 0;
    }

    .overlay-content {
      padding: clamp(20px, 2.1vw, 32px);
    }

    .overlay-content h1 {
      max-width: 16.5ch;
    }

    .brand-block {
      gap: 10px;
    }
  }

  @media (max-width: 900px) {
    .forside1,
    .hero-image {
      width: calc(100% - 24px);
      min-height: 52vh;
      border-radius: 18px;
    }

    .overlay-content {
      padding: 16px;
      background: linear-gradient(180deg, rgba(0, 0, 0, 0.45) 0%, rgba(0, 0, 0, 0.18) 48%, rgba(0, 0, 0, 0.42) 100%);
    }

    .overlay-content h1 {
      max-width: 16.5ch;
      font-size: 37px;
    }

    .brand-block p {
      max-width: 22ch;
      font-size: 20px;
    }
  }
</style>
